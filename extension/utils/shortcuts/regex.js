export const rx = (p, f) => new RegExp(p, f); export const esc = s => s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'); export const mt = (s, r) => s.match(r); export const ma = (s, r) => s.matchAll(r); export const ts = (r, s) => r.test(s); export const ex = (r, s) => r.exec(s); export const rp = (s, r, n) => s.replace(r, n); export const ra = (s, r, n) => s.replaceAll(r, n); export const sp = (s, r) => s.split(r); export const sr = (s, r) => s.search(r);